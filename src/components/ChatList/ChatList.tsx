import React from 'react'
import { useSelector } from 'react-redux'

interface IChatList {
  onSelectChat: (chatId: string) => void
}

export const ChatList = ({onSelectChat}: IChatList) => {
  const chats
  return (
    <div>ChatList</div>
  )
}
